import { describe, expect, test } from "vitest";
//import { fireEvent, render, screen } from "@testing-library/vue";
//import Hamburger from "./Hamburger.vue";

describe("Hamburger tests", () => {
  test("hamburger test 1", () => {
    expect(true).toBe(true);
  });
});
